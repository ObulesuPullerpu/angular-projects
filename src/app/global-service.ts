import { Injectable } from '@angular/core';
import {  Observer, Subscriber } from 'rxjs';

@Injectable({
  providedIn: 'root',
})
export class GlobalService {
   data=[]=[];

//    (destination?: Subscriber<any> | Observer<any>)

//    const ber = new Subscriber((val)=>{
// data.push(val);
//   });
  
};
